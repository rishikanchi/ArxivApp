<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Arxiv Research Explorer</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <style>
        .hide { 
            position: absolute; 
            top: -1px; 
            left: -1px; 
            width: 1px; 
            height: 1px; 
        }
        @keyframes fadeInOut {
            0%, 100% { opacity: 0; }
            50% { opacity: 1; }
        }
        .animate-fadeInOut {
            animation: fadeInOut 2s ease-in-out infinite;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        .animate-spin {
            animation: spin 1s linear infinite;
        }
        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }
        .content-wrapper {
            flex: 1 0 auto;
            display: flex;
            flex-direction: column;
            position: relative;
            z-index: 1;
        }
        .signup-wrapper {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem 1rem;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            z-index: 50;
            background: inherit;
        }
        footer {
            flex-shrink: 0;
        }
        .disabled-input {
            background-color: #f3f4f6;
            cursor: not-allowed;
            opacity: 0.7;
        }
        .hidden {
            display: none !important;
        }
        .chart-container {
            display: flex;
            width: 100%;
            max-width: 6xl;
            margin-top: 1.5rem;
            margin-bottom: 3rem;
            background: white;
            border-radius: 0.5rem;
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
            transform: translateZ(0);
            transition: box-shadow 0.3s;
            height: 600px;
        }
        
        .chart-container:hover {
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
        }
        
        #chart {
            flex: 1;
            min-width: 0;
        }
        
        .paper-details {
            width: 350px;
            border-left: 1px solid #e5e7eb;
            background: white;
            padding: 1.5rem;
            overflow-y: auto;
            display: none;
            transition: transform 0.3s ease-in-out;
            transform: translateX(100%);
        }
        
        .paper-details.active {
            display: block;
            transform: translateX(0);
        }
        
        .paper-title {
            font-size: 1.25rem;
            font-weight: 600;
            color: #1f2937;
            margin-bottom: 1rem;
        }
        
        .paper-category {
            display: inline-block;
            padding: 0.25rem 0.75rem;
            background: #e5e7eb;
            border-radius: 9999px;
            font-size: 0.875rem;
            color: #4b5563;
            margin-bottom: 1rem;
        }
        
        .paper-description {
            color: #4b5563;
            line-height: 1.5;
            margin-bottom: 1.5rem;
        }
        
        .paper-link {
            display: inline-flex;
            align-items: center;
            padding: 0.5rem 1rem;
            background: #4f46e5;
            color: white;
            border-radius: 0.375rem;
            text-decoration: none;
            font-weight: 500;
            transition: background-color 0.2s;
        }
        
        .paper-link:hover {
            background: #4338ca;
        }
        
        .paper-link i {
            margin-left: 0.5rem;
        }

        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 1000;
        }

        .modal-content {
            background-color: white;
            margin: 2rem auto;
            padding: 1.5rem;
            border-radius: 0.5rem;
            max-width: 800px;
            max-height: 80vh;
            overflow-y: auto;
            position: relative;
        }

        .saved-paper-card {
            border: 1px solid #e5e7eb;
            border-radius: 0.5rem;
            padding: 1rem;
            margin-bottom: 1rem;
            transition: transform 0.2s;
        }

        .saved-paper-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        .close-button {
            position: absolute;
            top: 1rem;
            right: 1rem;
            font-size: 1.5rem;
            cursor: pointer;
            color: #4b5563;
        }

        .close-button:hover {
            color: #1f2937;
        }

        .empty-state {
            text-align: center;
            padding: 2rem;
            color: #6b7280;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-100 font-sans">
    <div class="content-wrapper">
        <header class="bg-white shadow-md">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
                <div class="flex justify-between items-center">
                    <h1 class="text-4xl font-extrabold text-indigo-600 cursor-pointer" onclick="showMainPage()">
                        Arxiv Research Explorer
                    </h1>
                    <div id="authButtons" class="flex gap-4">
                        <button onclick="toggleLogin()" id="loginBtn" 
                                class="bg-white hover:bg-gray-100 text-indigo-600 font-bold py-2 px-6 rounded-lg focus:outline-none focus:shadow-outline transition duration-300 flex items-center gap-2 border-2 border-indigo-600">
                            <i class="fas fa-sign-in-alt"></i>
                            Login
                        </button>
                        <button onclick="toggleSignup()" id="signupBtn" 
                                class="bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-2 px-6 rounded-lg focus:outline-none focus:shadow-outline transition duration-300 flex items-center gap-2">
                            <i class="fas fa-user-plus"></i>
                            Sign Up
                        </button>
                    </div>
                    <div id="userMenu" class="flex gap-4 hidden">
                        <button onclick="showMyPapers()" 
                                class="bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-2 px-6 rounded-lg focus:outline-none focus:shadow-outline transition duration-300 flex items-center gap-2">
                            <i class="fas fa-file-alt"></i>
                            My Papers
                        </button>
                        <button onclick="showAccountSettings()" 
                                class="bg-gray-600 hover:bg-gray-700 text-white font-bold py-2 px-6 rounded-lg focus:outline-none focus:shadow-outline transition duration-300 flex items-center gap-2">
                            <i class="fas fa-cog"></i>
                            Account Settings
                        </button>
                        <button onclick="handleLogout()" 
                                class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-6 rounded-lg focus:outline-none focus:shadow-outline transition duration-300 flex items-center gap-2">
                            <i class="fas fa-sign-out-alt"></i>
                            Logout
                        </button>
                    </div>
                </div>
            </div>
        </header>

        <div id="mainContent" class="flex-1">
            <main class="flex flex-col items-center mt-10 px-4 sm:px-6 lg:px-8">
                <iframe name="hiddenFrame" class="hide"></iframe>
                
                <div id="loginMessage" class="hidden bg-yellow-100 border-l-4 border-yellow-500 p-4 mb-6 max-w-2xl w-full">
                    <div class="flex">
                        <div class="flex-shrink-0">
                            <i class="fas fa-exclamation-circle text-yellow-500"></i>
                        </div>
                        <div class="ml-3">
                            <p class="text-sm text-yellow-700">
                                Please sign up or log in to start exploring research papers.
                            </p>
                        </div>
                    </div>
                </div>
                
                <div class="bg-white shadow-xl rounded-lg p-8 max-w-2xl w-full mb-12 transform hover:scale-105 transition-transform duration-300">
                    <form action="/" method="POST" class="space-y-6">
                        <div>
                            <label for="textInput" class="block text-gray-700 text-lg font-semibold mb-2">
                                What are you researching today?
                            </label>
                            <textarea id="textInput" 
                                    placeholder="Enter your research query here..." 
                                    rows="4" 
                                    name="textInput" 
                                    class="w-full px-3 py-2 text-gray-700 border rounded-lg focus:outline-none focus:border-indigo-500 resize-none" 
                                    required></textarea>
                        </div>
                        <div class="flex justify-end">
                            <button type="submit" class="bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-6 rounded-lg focus:outline-none focus:shadow-outline transition duration-300 flex items-center">
                                <i class="fas fa-search mr-2"></i>
                                Explore
                            </button>
                        </div>
                    </form>
                </div>
                
                <div class="chart-container">
                    <div id="chart"></div>
                    <div class="paper-details">
                        <h3 class="paper-title">Select a paper to view details</h3>
                        <span class="paper-category"></span>
                        <p class="paper-description"></p>
                        <div class="flex flex-col items-center space-y-4 w-full mt-6 mb-4">
                            <a href="#" class="paper-link w-4/5 flex items-center justify-center" target="_blank">
                                <span>View on arXiv</span>
                                <i class="fas fa-external-link-alt ml-2"></i>
                            </a>
                            <button onclick="savePaper()" class="w-4/5 bg-green-600 hover:bg-green-700 text-white font-bold py-2 px-4 rounded-lg focus:outline-none focus:shadow-outline transition duration-300 flex items-center justify-center gap-2">
                                <i class="fas fa-bookmark"></i>
                                <span>Save</span>
                            </button>
                        </div>
                    </div>
                </div>
            </main>
        </div>

        <div id="signupContent" class="hidden signup-wrapper">
            <div class="bg-white shadow-xl rounded-lg p-8 max-w-md w-full transform hover:scale-105 transition-transform duration-300">
                <h2 class="text-2xl font-bold text-gray-800 mb-6 text-center">Create Your Account</h2>
                
                <form id="signupForm" class="space-y-6">
                    <div>
                        <label for="fullName" class="block text-gray-700 text-sm font-semibold mb-2">Full Name</label>
                        <div class="relative">
                            <i class="fas fa-user absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
                            <input type="text" id="fullName" required
                                class="w-full pl-10 pr-3 py-2 text-gray-700 border rounded-lg focus:outline-none focus:border-indigo-500"
                                placeholder="John Doe">
                        </div>
                    </div>

                    <div>
                        <label for="email" class="block text-gray-700 text-sm font-semibold mb-2">Email Address</label>
                        <div class="relative">
                            <i class="fas fa-envelope absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
                            <input type="email" id="email" required
                                class="w-full pl-10 pr-3 py-2 text-gray-700 border rounded-lg focus:outline-none focus:border-indigo-500"
                                placeholder="john@example.com">
                        </div>
                    </div>

                    <div>
                        <label for="password" class="block text-gray-700 text-sm font-semibold mb-2">Password</label>
                        <div class="relative">
                            <i class="fas fa-lock absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
                            <input type="password" id="password" required
                                class="w-full pl-10 pr-3 py-2 text-gray-700 border rounded-lg focus:outline-none focus:border-indigo-500"
                                placeholder="••••••••">
                        </div>
                    </div>

                    <div class="flex items-center">
                        <input type="checkbox" id="terms" required 
                               class="h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 rounded">
                        <label for="terms" class="ml-2 block text-sm text-gray-700">
                            I agree to the <a href="#" class="text-indigo-600 hover:text-indigo-500">Terms and Conditions</a>
                        </label>
                    </div>

                    <div class="pt-4">
                        <button type="submit" 
                                class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-6 rounded-lg focus:outline-none focus:shadow-outline transition duration-300 flex items-center justify-center gap-2">
                            <i class="fas fa-user-plus"></i>
                            Create Account
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <div id="loginContent" class="hidden signup-wrapper">
            <div class="bg-white shadow-xl rounded-lg p-8 max-w-md w-full transform hover:scale-105 transition-transform duration-300">
                <h2 class="text-2xl font-bold text-gray-800 mb-6 text-center">Login to Your Account</h2>
                
                <form id="loginForm" class="space-y-6">
                    <div>
                        <label for="loginEmail" class="block text-gray-700 text-sm font-semibold mb-2">Email Address</label>
                        <div class="relative">
                            <i class="fas fa-envelope absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
                            <input type="email" id="loginEmail" required
                                class="w-full pl-10 pr-3 py-2 text-gray-700 border rounded-lg focus:outline-none focus:border-indigo-500"
                                placeholder="john@example.com">
                        </div>
                    </div>

                    <div>
                        <label for="loginPassword" class="block text-gray-700 text-sm font-semibold mb-2">Password</label>
                        <div class="relative">
                            <i class="fas fa-lock absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
                            <input type="password" id="loginPassword" required
                            class="w-full pl-10 pr-3 py-2 text-gray-700 border rounded-lg focus:outline-none focus:border-indigo-500"
                            placeholder="••••••••">
                    </div>
                </div>

                <div class="flex items-center justify-between">
                    <div class="flex items-center">
                        <input type="checkbox" id="remember" 
                               class="h-4 w-4 text-indigo-600 focus:ring-indigo-500 border-gray-300 rounded">
                        <label for="remember" class="ml-2 block text-sm text-gray-700">
                            Remember me
                        </label>
                    </div>
                    <a href="#" class="text-sm text-indigo-600 hover:text-indigo-500">Forgot password?</a>
                </div>

                <div class="pt-4">
                    <button type="submit" 
                            class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-6 rounded-lg focus:outline-none focus:shadow-outline transition duration-300 flex items-center justify-center gap-2">
                        <i class="fas fa-sign-in-alt"></i>
                        Login
                    </button>
                </div>
            </form>
        </div>
    </div>
    <div id="myPapersModal" class="modal">
        <div class="modal-content">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-2xl font-bold text-gray-800">My Saved Papers</h2>
                <i class="fas fa-times close-button" onclick="closeMyPapers()"></i>
            </div>
            
            <div id="savedPapersList">
                <!-- Papers will be dynamically inserted here -->
            </div>
        </div>
    </div>
</div>

<footer class="bg-white shadow-md py-6">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center text-gray-600">
        <p>&copy; 2024 Arxiv Research Explorer. All rights reserved.</p>
    </div>
</footer>

<script src="/js/lcjs.iife.js"></script>
<script src="/js/chart.js"></script>
<script>
    // Initialize savedArr if it doesn't exist
    let savedArr = [];

    // Update the savePaper function to include the arXiv URL and clean the title
    function savePaper() {
        // Get the title and remove "Paper Title: " prefix if it exists
        const fullTitle = document.querySelector('.paper-title').textContent;
        const cleanTitle = fullTitle.replace(/^Paper Title:\s*/i, '');
        
        const category = document.querySelector('.paper-category').textContent;
        const arxivUrl = document.querySelector('.paper-link').href;
        
        // Create paper object with cleaned title
        const paper = {
            title: cleanTitle,
            category: category,
            arxivUrl: arxivUrl,
            savedAt: new Date().toISOString()
        };
        
        // Check if paper is already saved (using cleaned title)
        const isPaperSaved = savedArr.some(savedPaper => savedPaper.title === cleanTitle);
        
        if (!isPaperSaved) {
            // Add to array
            savedArr.push(paper);
            
            // Visual feedback
            const saveButton = document.querySelector('.paper-details button');
            const originalBg = saveButton.classList.contains('bg-green-600');
            
            // Change button appearance temporarily
            if (originalBg) {
                saveButton.classList.remove('bg-green-600', 'hover:bg-green-700');
                saveButton.classList.add('bg-blue-600', 'hover:bg-blue-700');
                saveButton.innerHTML = '<i class="fas fa-check"></i><span>Saved!</span>';
                
                // Revert after 2 seconds
                setTimeout(() => {
                    saveButton.classList.remove('bg-blue-600', 'hover:bg-blue-700');
                    saveButton.classList.add('bg-green-600', 'hover:bg-green-700');
                    saveButton.innerHTML = '<i class="fas fa-bookmark"></i><span>Save</span>';
                }, 2000);
            }
            
            console.log('Paper saved:', paper);
            console.log('Current savedArr:', savedArr);
        } else {
            // Paper already saved - provide feedback
            alert('This paper is already in your saved collection!');
        }
    }

    // Add a function to clean title text
    function cleanPaperTitle(title) {
        return title.replace(/^Paper Title:\s*/i, '');
    }

    // Add a mutation observer to watch for changes in the paper details
    function initializePaperDetailsObserver() {
        const paperDetails = document.querySelector('.paper-details');
        const observer = new MutationObserver((mutations) => {
            mutations.forEach((mutation) => {
                if (mutation.target.classList.contains('paper-title')) {
                    const title = mutation.target.textContent;
                    if (title.startsWith('Paper Title:')) {
                        mutation.target.textContent = cleanPaperTitle(title);
                    }
                }
            });
        });
        
        observer.observe(paperDetails, {
            subtree: true,
            childList: true,
            characterData: true
        });
    }

    // Call the observer initialization when the document is loaded
    document.addEventListener('DOMContentLoaded', function() {
        initializePaperDetailsObserver();
        checkAuthState();
    });

    // Update the showMyPapers function with the arXiv button
    function showMyPapers() {
        const modal = document.getElementById('myPapersModal');
        const papersList = document.getElementById('savedPapersList');
        
        // Clear existing content
        papersList.innerHTML = '';
        
        if (savedArr.length === 0) {
            papersList.innerHTML = `
                <div class="empty-state">
                    <i class="fas fa-book-open text-4xl mb-4"></i>
                    <p class="text-lg">You haven't saved any papers yet.</p>
                    <p class="text-sm mt-2">Papers you save will appear here.</p>
                </div>
            `;
        } else {
            // Sort papers by saved date, most recent first
            const sortedPapers = [...savedArr].sort((a, b) => 
                new Date(b.savedAt) - new Date(a.savedAt)
            );
            
            sortedPapers.forEach(paper => {
                const paperCard = document.createElement('div');
                paperCard.className = 'saved-paper-card';
                
                const savedDate = new Date(paper.savedAt).toLocaleDateString('en-US', {
                    year: 'numeric',
                    month: 'short',
                    day: 'numeric'
                });
                
                paperCard.innerHTML = `
                    <div class="flex justify-between items-start">
                        <div class="flex-grow">
                            <h3 class="text-lg font-semibold text-gray-800 mb-2">${paper.title}</h3>
                            <span class="inline-block bg-gray-100 rounded-full px-3 py-1 text-sm text-gray-600">
                                ${paper.category}
                            </span>
                        </div>
                        <div class="flex items-start space-x-2">
                            <a href="${paper.arxivUrl}" target="_blank" 
                            class="bg-indigo-600 hover:bg-indigo-700 text-white px-3 py-1 rounded-lg transition-colors">
                                <i class="fas fa-external-link-alt"></i>
                                <span class="ml-1">arXiv</span>
                            </a>
                            <button onclick="removeSavedPaper('${paper.title}')" 
                                    class="text-red-500 hover:text-red-700 transition-colors px-2">
                                <i class="fas fa-trash"></i>
                            </button>
                        </div>
                    </div>
                    <div class="mt-4 text-sm text-gray-500">
                        Saved on ${savedDate}
                    </div>
                `;
                
                papersList.appendChild(paperCard);
            });
        }
        
        modal.style.display = 'block';
    }

    // Loading messages rotation
    const loadingMessages = [
        "Getting things prepped for you...",
        "Crunching the numbers...",
        "Analyzing research patterns...",
        "Connecting the dots...",
        "Unearthing hidden insights...",
        "Mapping the knowledge landscape...",
        "Synthesizing information...",
        "Discovering breakthrough connections..."
    ];

    let currentMessageIndex = 0;

    function updateLoadingMessage() {
        const loadingMessageElement = document.getElementById('loadingMessage');
        if (loadingMessageElement) {
            loadingMessageElement.textContent = loadingMessages[currentMessageIndex];
            currentMessageIndex = (currentMessageIndex + 1) % loadingMessages.length;
        }
    }

    setInterval(updateLoadingMessage, 3000);
    updateLoadingMessage();

    // Check auth state on page load
    document.addEventListener('DOMContentLoaded', function() {
        checkAuthState();
    });

    function checkAuthState() {
        const isAuthenticated = localStorage.getItem('isAuthenticated') === 'true';
        const authButtons = document.getElementById('authButtons');
        const userMenu = document.getElementById('userMenu');
        const textInput = document.getElementById('textInput');
        const loginMessage = document.getElementById('loginMessage');
        
        if (isAuthenticated) {
            authButtons.classList.add('hidden');
            userMenu.classList.remove('hidden');
            textInput.disabled = false;
            textInput.classList.remove('disabled-input');
            loginMessage.classList.add('hidden');
        } else {
            authButtons.classList.remove('hidden');
            userMenu.classList.add('hidden');
            textInput.disabled = true;
            textInput.classList.add('disabled-input');
            loginMessage.classList.remove('hidden');
        }
    }

    function toggleLogin() {
        const mainContent = document.getElementById('mainContent');
        const loginContent = document.getElementById('loginContent');
        const signupContent = document.getElementById('signupContent');
        const loginBtn = document.getElementById('loginBtn');
        const signupBtn = document.getElementById('signupBtn');

        if (loginContent.classList.contains('hidden')) {
            mainContent.classList.add('hidden');
            loginContent.classList.remove('hidden');
            signupContent.classList.add('hidden');
            document.body.style.overflow = 'hidden';
            loginBtn.innerHTML = '<i class="fas fa-home"></i> Back to Home';
            signupBtn.innerHTML = '<i class="fas fa-user-plus"></i> Sign Up';
        } else {
            mainContent.classList.remove('hidden');
            loginContent.classList.add('hidden');
            document.body.style.overflow = '';
            loginBtn.innerHTML = '<i class="fas fa-sign-in-alt"></i> Login';
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }
    }

    function toggleSignup() {
        const mainContent = document.getElementById('mainContent');
        const signupContent = document.getElementById('signupContent');
        const loginContent = document.getElementById('loginContent');
        const signupBtn = document.getElementById('signupBtn');
        const loginBtn = document.getElementById('loginBtn');

        if (signupContent.classList.contains('hidden')) {
            mainContent.classList.add('hidden');
            signupContent.classList.remove('hidden');
            loginContent.classList.add('hidden');
            document.body.style.overflow = 'hidden';
            signupBtn.innerHTML = '<i class="fas fa-home"></i> Back to Home';
            loginBtn.innerHTML = '<i class="fas fa-sign-in-alt"></i> Login';
        } else {
            mainContent.classList.remove('hidden');
            signupContent.classList.add('hidden');
            document.body.style.overflow = '';
            signupBtn.innerHTML = '<i class="fas fa-user-plus"></i> Sign Up';
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }
    }

    // Form handlers
    const signupForm = document.getElementById('signupForm');
    if (signupForm) {
        signupForm.addEventListener('submit', function(e) {
            e.preventDefault();
            localStorage.setItem('isAuthenticated', 'true');
            localStorage.setItem('userEmail', document.getElementById('email').value);
            localStorage.setItem('userName', document.getElementById('fullName').value);
            signupForm.reset();
            checkAuthState();
            showMainPage();
        });
    }

    const loginForm = document.getElementById('loginForm');
    if (loginForm) {
        loginForm.addEventListener('submit', function(e) {
            e.preventDefault();
            localStorage.setItem('isAuthenticated', 'true');
            localStorage.setItem('userEmail', document.getElementById('loginEmail').value);
            localStorage.setItem('userName', 'User');
            loginForm.reset();
            checkAuthState();
            showMainPage();
        });
    }

    function handleLogout() {
        localStorage.removeItem('isAuthenticated');
        localStorage.removeItem('userEmail');
        localStorage.removeItem('userName');
        checkAuthState();
        showMainPage();
    }

    function showMainPage() {
        const mainContent = document.getElementById('mainContent');
        const signupContent = document.getElementById('signupContent');
        const loginContent = document.getElementById('loginContent');
        const signupBtn = document.getElementById('signupBtn');
        const loginBtn = document.getElementById('loginBtn');
        
        mainContent.classList.remove('hidden');
        signupContent.classList.add('hidden');
        loginContent.classList.add('hidden');
        document.body.style.overflow = '';
        signupBtn.innerHTML = '<i class="fas fa-user-plus"></i> Sign Up';
        loginBtn.innerHTML = '<i class="fas fa-sign-in-alt"></i> Login';
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }

    function showAccountSettings() {
        const userEmail = localStorage.getItem('userEmail');
        alert(`Account Settings for ${userEmail} - Feature coming soon!`);
    }

    // Research form submission handler
    const researchForm = document.querySelector('form[action="/"]');
    if (researchForm) {
        researchForm.addEventListener('submit', function(e) {
            const isAuthenticated = localStorage.getItem('isAuthenticated') === 'true';
            if (!isAuthenticated) {
                e.preventDefault();
                alert('Please sign up or log in to submit research queries.');
                return false;
            }
        });
    }

    // Function to remove a saved paper
    function removeSavedPaper(title) {
        if (confirm('Are you sure you want to remove this paper from your saved collection?')) {
            savedArr = savedArr.filter(paper => paper.title !== title);
            showMyPapers(); // Refresh the display
        }
    }

    // Function to close the modal
    function closeMyPapers() {
        const modal = document.getElementById('myPapersModal');
        modal.style.display = 'none';
    }

    // Close modal when clicking outside
    window.onclick = function(event) {
        const modal = document.getElementById('myPapersModal');
        if (event.target === modal) {
            modal.style.display = 'none';
        }
    }    
</script>
</body>
</html>